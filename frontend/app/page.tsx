import {
  ArrowRight,
  Satellite,
  Sparkles,
  TrendingUp,
  Database,
  FileWarning,
  ClipboardList,
  Landmark,
  CheckCircle2,
} from "lucide-react";
import Footer from "@/components/Footer";
import { AIIcon, ForecastIcon, GlobeIcon } from "@/components/icons/FeatureIcons";
import Navbar from "@/components/Navbar";

const features = [
  {
    icon: ForecastIcon,
    title: "Agentic Cash Flow Forecasting",
    description:
      "Autonomous agents continuously model income volatility from transaction history, seasonal cycles, and local demand — surfacing liquidity gaps weeks before they hit.",
  },
  {
    icon: AIIcon,
    title: "XAI Risk Explanations",
    description:
      "Every risk score ships with a plain-language rationale generated by Gemini, so loan officers can defend decisions to auditors, regulators, and borrowers alike.",
  },
  {
    icon: GlobeIcon,
    title: "AlphaEarth Climate Monitoring",
    description:
      "Satellite-derived soil moisture, rainfall, and vegetation indices from Google AlphaEarth flag climate-driven default risk before it shows up in repayment data.",
  },
];

const legacyPoints = [
  "Static credit bureau scores, updated quarterly at best",
  "No visibility into informal or cash-based income",
  "Blind to climate and weather shocks on repayment ability",
  "Risk flagged only after a payment is missed",
];

const gramosPoints = [
  "BigQuery-fused transaction logs, updated in real time",
  "Alternative data: mobile money, utility, and input purchases",
  "AlphaEarth satellite metrics on rainfall, soil, and vegetation",
  "Gemini reasoning flags risk 30+ days before default",
];

export default function Home() {
  return (
    <div id="top" className="flex flex-1 flex-col bg-onyx text-slate-50">
      <Navbar />

      <main className="flex-1">
        {/* Hero */}
        <section className="relative overflow-hidden">
          <div
            aria-hidden
            className="pointer-events-none absolute inset-0 [background:radial-gradient(60%_50%_at_50%_0%,rgba(251,191,36,0.12)_0%,rgba(18,20,18,0)_100%)]"
          />
          <div className="relative mx-auto flex w-full max-w-7xl flex-col items-center px-6 py-24 text-center lg:px-8 lg:py-32">
            <h1 className="max-w-4xl text-balance text-4xl font-semibold tracking-tight text-slate-50 sm:text-5xl lg:text-6xl">
              The AI Operating System for{" "}
              <span className="text-amber-400">Rural Financial Intelligence</span>
            </h1>
            <p className="mt-6 max-w-2xl text-pretty text-lg leading-8 text-slate-400">
              Predict NPA risk before it happens using climate satellite data and
              alternative transaction logs.
            </p>
            <div className="mt-10 flex flex-col items-center gap-4 sm:flex-row">
              <a
                href="#demo"
                className="inline-flex cursor-pointer items-center gap-2 rounded-md bg-amber-400 px-6 py-3 text-sm font-semibold text-onyx transition hover:bg-amber-300"
              >
                Request Demo
                <ArrowRight className="h-4 w-4" />
              </a>
              <a
                href="#architecture"
                className="inline-flex cursor-pointer items-center gap-2 rounded-md border border-gray-700 px-6 py-3 text-sm font-semibold text-slate-200 transition hover:border-gray-500 hover:bg-card"
              >
                See how it works
              </a>
            </div>

            {/* Stat row */}
            <dl className="mt-20 grid w-full max-w-3xl grid-cols-1 gap-8 border-t border-gray-700 pt-10 sm:grid-cols-3">
              <div>
                <dt className="text-sm text-slate-400">Earlier risk detection</dt>
                <dd className="mt-1 text-3xl font-semibold text-slate-50">
                  30+ days
                </dd>
              </div>
              <div>
                <dt className="text-sm text-slate-400">Data sources fused</dt>
                <dd className="mt-1 text-3xl font-semibold text-slate-50">
                  BigQuery + AlphaEarth
                </dd>
              </div>
              <div>
                <dt className="text-sm text-slate-400">Every score</dt>
                <dd className="mt-1 text-3xl font-semibold text-slate-50">
                  Explainable by Gemini
                </dd>
              </div>
            </dl>
          </div>
        </section>

        {/* Architecture */}
        <section id="architecture" className="py-24 sm:py-32">
          <div className="mx-auto w-full max-w-7xl px-6 lg:px-8">
            <div className="mx-auto max-w-2xl text-center">
              <h2 className="text-sm font-semibold uppercase tracking-wider text-amber-400">
                Architecture
              </h2>
              <p className="mt-2 text-3xl font-semibold tracking-tight text-slate-50 sm:text-4xl">
                Legacy credit scoring can&rsquo;t see rural risk coming
              </p>
              <p className="mt-4 text-lg text-slate-400">
                GramOS fuses structured transaction data with real-time earth
                observation, then reasons over both with Gemini.
              </p>
            </div>

            <div className="mt-16 grid grid-cols-1 gap-8 lg:grid-cols-2">
              {/* Legacy */}
              <div className="rounded-2xl border border-gray-700 bg-card p-8">
                <div className="flex items-center gap-3">
                  <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-onyx">
                    <Landmark className="h-5 w-5 text-slate-500" />
                  </div>
                  <h3 className="text-lg font-semibold text-slate-400">
                    Legacy Manual Credit Scoring
                  </h3>
                </div>
                <ul className="mt-6 space-y-4">
                  {legacyPoints.map((point) => (
                    <li key={point} className="flex items-start gap-3">
                      <FileWarning className="mt-0.5 h-4 w-4 shrink-0 text-slate-500" />
                      <span className="text-sm leading-6 text-slate-400">
                        {point}
                      </span>
                    </li>
                  ))}
                </ul>
              </div>

              {/* GramOS */}
              <div className="relative rounded-2xl border border-amber-400/30 bg-card p-8 shadow-xl shadow-black/20">
                <div
                  aria-hidden
                  className="pointer-events-none absolute inset-0 rounded-2xl [background:radial-gradient(60%_60%_at_100%_0%,rgba(251,191,36,0.10)_0%,rgba(30,34,31,0)_100%)]"
                />
                <div className="relative flex items-center gap-3">
                  <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-amber-400/10">
                    <Database className="h-5 w-5 text-amber-400" />
                  </div>
                  <h3 className="text-lg font-semibold text-slate-50">
                    The GramOS Framework
                  </h3>
                </div>
                <ul className="relative mt-6 space-y-4">
                  {gramosPoints.map((point) => (
                    <li key={point} className="flex items-start gap-3">
                      <CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-amber-400" />
                      <span className="text-sm leading-6 text-slate-300">
                        {point}
                      </span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>

            {/* Pipeline strip */}
            <div className="mt-10 flex flex-col items-stretch justify-center gap-3 rounded-2xl border border-gray-700 bg-card p-6 sm:flex-row sm:items-center">
              <PipelineStep
                icon={ClipboardList}
                label="BigQuery Transaction Logs"
              />
              <PipelineArrow />
              <PipelineStep icon={Satellite} label="AlphaEarth Satellite Metrics" />
              <PipelineArrow />
              <PipelineStep
                icon={Sparkles}
                label="Gemini Reasoning Engine"
                highlight
              />
              <PipelineArrow />
              <PipelineStep icon={TrendingUp} label="Explainable Risk Score" />
            </div>
          </div>
        </section>

        {/* Features */}
        <section id="features" className="py-24 sm:py-32">
          <div className="mx-auto w-full max-w-7xl px-6 lg:px-8">
            <div className="mx-auto max-w-2xl text-center">
              <h2 className="text-sm font-semibold uppercase tracking-wider text-amber-400">
                Platform
              </h2>
              <p className="mt-2 text-3xl font-semibold tracking-tight text-slate-50 sm:text-4xl">
                One platform, three ways to de-risk your portfolio
              </p>
            </div>

            <div className="mt-16 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
              {features.map(({ icon: Icon, title, description }) => (
                <div
                  key={title}
                  className="group rounded-2xl border border-gray-700 bg-card p-8 transition hover:border-amber-400/50"
                >
                  <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-onyx transition group-hover:bg-amber-400">
                    <Icon className="h-6 w-6 text-amber-400 transition group-hover:text-onyx" />
                  </div>
                  <h3 className="mt-6 text-lg font-semibold text-slate-50">
                    {title}
                  </h3>
                  <p className="mt-3 text-sm leading-6 text-slate-400">
                    {description}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </section>

        {/* CTA / Demo */}
        <section id="demo" className="border-t border-gray-700 bg-card">
          <div className="mx-auto flex w-full max-w-7xl flex-col items-center px-6 py-24 text-center lg:px-8">
            <p className="text-sm font-semibold uppercase tracking-wider text-amber-400">
              Get started
            </p>
            <h2 className="mt-2 max-w-xl text-3xl font-semibold tracking-tight text-slate-50 sm:text-4xl">
              See GramOS flag risk in your portfolio
            </h2>
            <p className="mt-4 max-w-xl text-lg text-slate-400">
              Book a walkthrough with our team and bring your own transaction
              sample — we&rsquo;ll show you the explainable risk output live.
            </p>
            <a
              href="mailto:demo@gramos.ai"
              className="mt-8 inline-flex cursor-pointer items-center gap-2 rounded-md bg-amber-400 px-6 py-3 text-sm font-semibold text-onyx transition hover:bg-amber-300"
            >
              Request Demo
              <ArrowRight className="h-4 w-4" />
            </a>
          </div>
        </section>
      </main>

      <Footer />
    </div>
  );
}

function PipelineStep({
  icon: Icon,
  label,
  highlight = false,
}: {
  icon: typeof Sparkles;
  label: string;
  highlight?: boolean;
}) {
  return (
    <div
      className={`flex flex-1 items-center gap-3 rounded-xl px-4 py-3 ${
        highlight ? "bg-amber-400/10" : ""
      }`}
    >
      <Icon
        className={`h-5 w-5 shrink-0 ${
          highlight ? "text-amber-400" : "text-slate-500"
        }`}
      />
      <span
        className={`text-sm font-medium ${
          highlight ? "text-slate-50" : "text-slate-400"
        }`}
      >
        {label}
      </span>
    </div>
  );
}

function PipelineArrow() {
  return (
    <ArrowRight className="mx-auto h-4 w-4 shrink-0 rotate-90 text-slate-600 sm:rotate-0" />
  );
}
