import {
  ArrowRight,
  Satellite,
  Sparkles,
  TrendingUp,
  Database,
  FileWarning,
  ClipboardList,
  Landmark,
  CheckCircle2,
} from "lucide-react";
import Image from "next/image";

const features = [
  {
    icon: TrendingUp,
    title: "Agentic Cash Flow Forecasting",
    description:
      "Autonomous agents continuously model income volatility from transaction history, seasonal cycles, and local demand — surfacing liquidity gaps weeks before they hit.",
  },
  {
    icon: Sparkles,
    title: "XAI Risk Explanations",
    description:
      "Every risk score ships with a plain-language rationale generated by Gemini, so loan officers can defend decisions to auditors, regulators, and borrowers alike.",
  },
  {
    icon: Satellite,
    title: "AlphaEarth Climate Monitoring",
    description:
      "Satellite-derived soil moisture, rainfall, and vegetation indices from Google AlphaEarth flag climate-driven default risk before it shows up in repayment data.",
  },
];

const legacyPoints = [
  "Static credit bureau scores, updated quarterly at best",
  "No visibility into informal or cash-based income",
  "Blind to climate and weather shocks on repayment ability",
  "Risk flagged only after a payment is missed",
];

const gramosPoints = [
  "BigQuery-fused transaction logs, updated in real time",
  "Alternative data: mobile money, utility, and input purchases",
  "AlphaEarth satellite metrics on rainfall, soil, and vegetation",
  "Gemini reasoning flags risk 30+ days before default",
];

export default function Home() {
  return (
    <div className="flex flex-1 flex-col bg-slate-50 text-slate-900">
      {/* Nav */}
      <header className="sticky top-0 z-50 border-b border-slate-200 bg-slate-50/80 backdrop-blur">
        <nav className="mx-auto flex w-full max-w-7xl items-center justify-between px-6 py-4 lg:px-8">
          <Image
            src="/gramos-logo.png"
            alt="GramOS"
            width={1051}
            height={907}
            className="h-14 w-auto"
            priority
          />
          <div className="hidden items-center gap-8 text-sm font-medium text-slate-600 md:flex">
            <a href="#architecture" className="transition hover:text-slate-900">
              Architecture
            </a>
            <a href="#features" className="transition hover:text-slate-900">
              Platform
            </a>
            <a href="#demo" className="transition hover:text-slate-900">
              Request Demo
            </a>
          </div>
          <a
            href="#demo"
            className="inline-flex items-center gap-1.5 rounded-md bg-slate-900 px-4 py-2 text-sm font-semibold text-slate-50 transition hover:bg-slate-800"
          >
            Request Demo
          </a>
        </nav>
      </header>

      <main className="flex-1">
        {/* Hero */}
        <section className="relative overflow-hidden bg-slate-900">
          <div
            aria-hidden
            className="pointer-events-none absolute inset-0 [background:radial-gradient(60%_50%_at_50%_0%,rgba(16,185,129,0.18)_0%,rgba(15,23,42,0)_100%)]"
          />
          <div className="relative mx-auto flex w-full max-w-7xl flex-col items-center px-6 py-28 text-center lg:px-8 lg:py-36">
            <div className="mb-6 inline-flex items-center gap-2 rounded-full border border-emerald-500/30 bg-emerald-500/10 px-4 py-1.5 text-xs font-medium text-emerald-400">
              <Sparkles className="h-3.5 w-3.5" />
              Built for the Google DeepMind AI for the Planet Accelerator
            </div>
            <h1 className="max-w-4xl text-balance text-4xl font-semibold tracking-tight text-slate-50 sm:text-5xl lg:text-6xl">
              The AI Operating System for{" "}
              <span className="text-emerald-500">Rural Financial Intelligence</span>
            </h1>
            <p className="mt-6 max-w-2xl text-pretty text-lg leading-8 text-slate-400">
              Predict NPA risk before it happens using climate satellite data and
              alternative transaction logs.
            </p>
            <div className="mt-10 flex flex-col items-center gap-4 sm:flex-row">
              <a
                href="#demo"
                className="inline-flex items-center gap-2 rounded-md bg-emerald-500 px-6 py-3 text-sm font-semibold text-slate-900 transition hover:bg-emerald-400"
              >
                Request Demo
                <ArrowRight className="h-4 w-4" />
              </a>
              <a
                href="#architecture"
                className="inline-flex items-center gap-2 rounded-md border border-slate-700 px-6 py-3 text-sm font-semibold text-slate-200 transition hover:border-slate-500 hover:bg-slate-800"
              >
                See how it works
              </a>
            </div>

            {/* Stat row */}
            <dl className="mt-20 grid w-full max-w-3xl grid-cols-1 gap-8 border-t border-slate-800 pt-10 sm:grid-cols-3">
              <div>
                <dt className="text-sm text-slate-500">Earlier risk detection</dt>
                <dd className="mt-1 text-3xl font-semibold text-slate-50">
                  30+ days
                </dd>
              </div>
              <div>
                <dt className="text-sm text-slate-500">Data sources fused</dt>
                <dd className="mt-1 text-3xl font-semibold text-slate-50">
                  BigQuery + AlphaEarth
                </dd>
              </div>
              <div>
                <dt className="text-sm text-slate-500">Every score</dt>
                <dd className="mt-1 text-3xl font-semibold text-slate-50">
                  Explainable by Gemini
                </dd>
              </div>
            </dl>
          </div>
        </section>

        {/* Architecture */}
        <section id="architecture" className="bg-slate-50 py-24 sm:py-32">
          <div className="mx-auto w-full max-w-7xl px-6 lg:px-8">
            <div className="mx-auto max-w-2xl text-center">
              <h2 className="text-sm font-semibold uppercase tracking-wider text-emerald-600">
                Architecture
              </h2>
              <p className="mt-2 text-3xl font-semibold tracking-tight text-slate-900 sm:text-4xl">
                Legacy credit scoring can&rsquo;t see rural risk coming
              </p>
              <p className="mt-4 text-lg text-slate-600">
                GramOS fuses structured transaction data with real-time earth
                observation, then reasons over both with Gemini.
              </p>
            </div>

            <div className="mt-16 grid grid-cols-1 gap-8 lg:grid-cols-2">
              {/* Legacy */}
              <div className="rounded-2xl border border-slate-200 bg-white p-8">
                <div className="flex items-center gap-3">
                  <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-slate-100">
                    <Landmark className="h-5 w-5 text-slate-500" />
                  </div>
                  <h3 className="text-lg font-semibold text-slate-500">
                    Legacy Manual Credit Scoring
                  </h3>
                </div>
                <ul className="mt-6 space-y-4">
                  {legacyPoints.map((point) => (
                    <li key={point} className="flex items-start gap-3">
                      <FileWarning className="mt-0.5 h-4 w-4 shrink-0 text-slate-400" />
                      <span className="text-sm leading-6 text-slate-500">
                        {point}
                      </span>
                    </li>
                  ))}
                </ul>
              </div>

              {/* GramOS */}
              <div className="relative rounded-2xl border border-slate-900 bg-slate-900 p-8 shadow-xl shadow-slate-900/10">
                <div
                  aria-hidden
                  className="pointer-events-none absolute inset-0 rounded-2xl [background:radial-gradient(60%_60%_at_100%_0%,rgba(16,185,129,0.15)_0%,rgba(15,23,42,0)_100%)]"
                />
                <div className="relative flex items-center gap-3">
                  <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-emerald-500/10">
                    <Database className="h-5 w-5 text-emerald-500" />
                  </div>
                  <h3 className="text-lg font-semibold text-slate-50">
                    The GramOS Framework
                  </h3>
                </div>
                <ul className="relative mt-6 space-y-4">
                  {gramosPoints.map((point) => (
                    <li key={point} className="flex items-start gap-3">
                      <CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-emerald-500" />
                      <span className="text-sm leading-6 text-slate-300">
                        {point}
                      </span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>

            {/* Pipeline strip */}
            <div className="mt-10 flex flex-col items-stretch justify-center gap-3 rounded-2xl border border-slate-200 bg-white p-6 sm:flex-row sm:items-center">
              <PipelineStep
                icon={ClipboardList}
                label="BigQuery Transaction Logs"
              />
              <PipelineArrow />
              <PipelineStep icon={Satellite} label="AlphaEarth Satellite Metrics" />
              <PipelineArrow />
              <PipelineStep
                icon={Sparkles}
                label="Gemini Reasoning Engine"
                highlight
              />
              <PipelineArrow />
              <PipelineStep icon={TrendingUp} label="Explainable Risk Score" />
            </div>
          </div>
        </section>

        {/* Features */}
        <section id="features" className="bg-white py-24 sm:py-32">
          <div className="mx-auto w-full max-w-7xl px-6 lg:px-8">
            <div className="mx-auto max-w-2xl text-center">
              <h2 className="text-sm font-semibold uppercase tracking-wider text-emerald-600">
                Platform
              </h2>
              <p className="mt-2 text-3xl font-semibold tracking-tight text-slate-900 sm:text-4xl">
                One platform, three ways to de-risk your portfolio
              </p>
            </div>

            <div className="mt-16 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
              {features.map(({ icon: Icon, title, description }) => (
                <div
                  key={title}
                  className="group rounded-2xl border border-slate-200 p-8 transition hover:border-slate-900"
                >
                  <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-slate-900 transition group-hover:bg-emerald-500">
                    <Icon className="h-6 w-6 text-emerald-500 transition group-hover:text-slate-900" />
                  </div>
                  <h3 className="mt-6 text-lg font-semibold text-slate-900">
                    {title}
                  </h3>
                  <p className="mt-3 text-sm leading-6 text-slate-600">
                    {description}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </section>

        {/* CTA / Demo */}
        <section id="demo" className="bg-slate-900">
          <div className="mx-auto flex w-full max-w-7xl flex-col items-center px-6 py-24 text-center lg:px-8">
            <p className="text-sm font-semibold uppercase tracking-wider text-emerald-500">
              Get started
            </p>
            <h2 className="mt-2 max-w-xl text-3xl font-semibold tracking-tight text-slate-50 sm:text-4xl">
              See GramOS flag risk in your portfolio
            </h2>
            <p className="mt-4 max-w-xl text-lg text-slate-400">
              Book a walkthrough with our team and bring your own transaction
              sample — we&rsquo;ll show you the explainable risk output live.
            </p>
            <a
              href="mailto:demo@gramos.ai"
              className="mt-8 inline-flex items-center gap-2 rounded-md bg-emerald-500 px-6 py-3 text-sm font-semibold text-slate-900 transition hover:bg-emerald-400"
            >
              Request Demo
              <ArrowRight className="h-4 w-4" />
            </a>
          </div>
        </section>
      </main>

      {/* Footer */}
      <footer className="border-t border-slate-200 bg-slate-50">
        <div className="mx-auto flex w-full max-w-7xl flex-col items-center justify-between gap-4 px-6 py-8 text-sm text-slate-500 sm:flex-row lg:px-8">
          <span>© {new Date().getFullYear()} GramOS. All rights reserved.</span>
          <span>Powered by Gemini &amp; Google AlphaEarth</span>
        </div>
      </footer>
    </div>
  );
}

function PipelineStep({
  icon: Icon,
  label,
  highlight = false,
}: {
  icon: typeof Sparkles;
  label: string;
  highlight?: boolean;
}) {
  return (
    <div
      className={`flex flex-1 items-center gap-3 rounded-xl px-4 py-3 ${
        highlight ? "bg-slate-900" : "bg-slate-50"
      }`}
    >
      <Icon
        className={`h-5 w-5 shrink-0 ${
          highlight ? "text-emerald-500" : "text-slate-500"
        }`}
      />
      <span
        className={`text-sm font-medium ${
          highlight ? "text-slate-50" : "text-slate-700"
        }`}
      >
        {label}
      </span>
    </div>
  );
}

function PipelineArrow() {
  return (
    <ArrowRight className="mx-auto h-4 w-4 shrink-0 rotate-90 text-slate-300 sm:rotate-0" />
  );
}
